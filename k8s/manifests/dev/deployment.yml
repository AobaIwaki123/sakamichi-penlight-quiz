apiVersion: apps/v1
kind: Deployment
metadata:
  name: penlight-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: penlight-dev
  template:
    metadata:
      labels:
        app: penlight-dev
    spec:
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: penlight-dev
          image: harbor.aooba.net/penlight/prod-view-penlight:dev
          volumeMounts:
            - name: gcp-sa-volume
              mountPath: /app/secret
              readOnly: true
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /app/secret/key.json
          ports:
            - containerPort: 3000
      volumes:
        - name: gcp-sa-volume
          secret:
            secretName: gcp-sa-key
